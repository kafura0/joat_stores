name: joat_hub
description: Joat Stores customer portal — shop across all your favourite stores.
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: "^3.5.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.0
  provider: ^6.1.0
  flutter_secure_storage: ^9.2.0
  firebase_messaging: ^15.0.0
  firebase_core: ^3.0.0
  cupertino_icons: ^1.0.8
  intl: ^0.19.0
  url_launcher: ^6.3.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
