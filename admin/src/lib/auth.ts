// lib/auth.ts
// JWT token management for the admin app.
// Full implementation in Story 1.8.
//
// RULE: JWT access token stored in memory only — never localStorage.
// Refresh token stored in httpOnly cookie (set by backend on login).

// TODO: Story 1.8 — implement token refresh, login, logout helpers
