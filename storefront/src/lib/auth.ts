// lib/auth.ts
// JWT token management for the storefront.
// Full implementation in Story 1.5.
//
// RULE: JWT access token stored in memory only — never localStorage.
// Refresh token stored in httpOnly cookie (set by backend on login).

// TODO: Story 1.5 — implement token refresh, login, logout helpers
