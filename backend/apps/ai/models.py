"""
AI Event capture model.

AIEvent is defined in apps.analytics.models — this file is intentionally
left as a namespace placeholder. Use the analytics.models.AIEvent model
for all event capture needs.

Story 8.2: Full AIEvent model (append-only behavioural event stream)
Story 11.1: Recommendation engine consumes AIEvent for collaborative filtering
"""
